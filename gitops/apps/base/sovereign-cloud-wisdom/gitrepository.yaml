# Flux source — polls the app repo for Helm chart changes
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: sovereign-cloud-wisdom
  namespace: flux-system
spec:
  interval: 5m
  url: https://github.com/lejeunen/sovereign-cloud-wisdom
  ref:
    branch: main
