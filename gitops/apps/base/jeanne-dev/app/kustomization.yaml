apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The showcase dogfood: Jeanne consumed from the released OCI chart (Phase 13). The base
# chart/persona/skills stay private; only this thin consumer is public (IP boundary, S8).
resources:
  - externalsecret-registry.yaml
  # - helmrepository.yaml      # Phase 13 Step 2 (OCI source for the released chart)
  # - helmrelease.yaml         # Phase 13 Step 2
  # - configmap-overlay.yaml   # Phase 13 Step 3 (showcase AGENTS overlay)
