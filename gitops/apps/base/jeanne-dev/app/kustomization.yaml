apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The showcase dogfood: Jeanne consumed from the released OCI chart (Phase 13). The base
# chart/persona/skills stay private; only this thin consumer is public (IP boundary, S8).
resources:
  - externalsecret-registry.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  # configmap-overlay.yaml: the showcase dogfoods the BASE persona (no customer overlay),
  # like the dev harness; the customer-overlay seam is exercised separately.
